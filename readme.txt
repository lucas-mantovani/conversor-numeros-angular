O projeto foi criado utilizando o framework Angular.

É necessário ter instalado na máquina o Node.js e o Angular CLI para inicializar o projeto.

Inicialização: 

- Abra o projeto no terminal na pasta principal; 
- Execute o comando 'ng server'; 
- Por fim basta entrar na URL que é mostrada após executar o comando 'ng server'.
